# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with this repository.

## 协作语言

- 使用英文进行分析和思考。
- 使用简体中文编写 Git 提交日志、代码注释和会话回复。
- 代码中的标识符、API 名称、库名称和命令参数遵循其原有约定，不为满足中文要求而翻译。

## 项目定位与技术边界

这是一个使用 Rust 编写的 yt-dlp 图形化工具，目标运行平台为 Windows 10 版本 1903 及之后的版本。

- GUI 必须使用 Slint 方案实现。
- 禁止引入或使用任何 WebView 组件及相关技术栈。
- 新增依赖、平台 API 和构建配置必须符合 Windows 10 1903+ 的兼容性要求。
- 不要根据项目名称或 `.gitignore` 推断未被代码和配置确认的架构；以实际源代码和 Cargo 配置为准。

## 设计系统

根目录 [`design.md`](design.md) 是 UI 主题、颜色、字号、字体、行高、字间距和无障碍规则的唯一设计来源。

- 修改或新增 Slint UI、主题逻辑、设置项或控件状态前，先阅读并遵守 `design.md`。
- 新增或修改设计令牌时，必须在同一变更中同步更新 `design.md`，不得在组件中私自创建未登记的颜色、字号、字体或间距。
- 设计系统仍必须通过 Slint 落地，不得使用 WebView、HTML、CSS 或浏览器内核替代。

## 仓库当前状态

当前 checkout 已包含 Rust/Slint 基础工程、共享 UI 设计系统和四路由基础契约。应用仍处于基础设施阶段，页面正文、SQLite 存储、yt-dlp 调用、CI 配置和完整运行流程尚未建立。

`.gitignore` 使用了 Cargo/Rust 风格的忽略规则（`target`、Rust 备份文件、MSVC `.pdb` 文件和 cargo-mutants 输出）。

## 代码格式

- Rust 代码统一使用 120 列最大宽度，格式化配置以仓库根目录的 `rustfmt.toml` 为准。
- 修改 Rust 代码后执行 `cargo fmt --check`，不得仅依赖编辑器的局部格式化结果。

## 命令

以下命令已基于当前 Cargo 工程验证：

```text
cargo fmt --check
cargo check
cargo test
cargo test --test design_system_contract
```

每个开发分支一旦修改代码、Slint 界面、构建脚本或 Cargo 配置，必须在该分支重新编译；至少执行一次 `cargo check`，不得仅依赖之前的编译结果。

应用运行命令为：

```text
cargo run
```

Windows 主题、DPI 和 UI 视觉行为需要在 Windows 环境中实际运行应用验证。

可用的仓库检查命令：

```text
git status
git diff
git log --oneline --decorate --all
git ls-tree -r --name-only HEAD
```

Cargo 工程建立后，应在这里记录经过验证的命令，包括运行单个测试文件的方法。

## 界面与 Windows 适配要求

- GUI 必须是由 Slint 渲染的原生 Windows 可视化窗口，禁止使用 WebView。
- 窗口必须支持放大、缩小和调整大小。
- 所有界面必须适配 Windows 的任意 DPI 缩放比例；100% 和 150% 是最低验证基线，禁止依赖固定像素尺寸假设。
- 页面布局必须明确区分窗口区域、滚动视口、滚动内容区域和页面组件四个几何层级；页面组件不得反向决定滚动视口的宽高。
- 使用 `PageScrollHost` 时，页面真实内容高度必须通过稳定契约传入滚动宿主；不要依赖嵌套 `@children` 的隐式首选高度推导滚动范围。
- 滚动内容的左右留白必须由滚动宿主统一提供。需要左右对称边距时，使用固定的左留白列、页面内容列和右留白列，页面卡片不得自行使用窗口全宽或无限横向拉伸。
- 页面根节点不得随意声明 `width: 100%`、`horizontal-stretch: 1` 或重复的宽度传递链；页面宽度应由父级内容容器单向约束，避免 `@children` 和布局引擎覆盖边界。
- 固定高度的文本必须显式设置与所在行相同的 `height`，并设置 `vertical-alignment: center`；仅设置垂直对齐而不设置文本高度不足以保证视觉居中。
- 新增页面必须同时验证窄窗口滚轮滚动、宽窗口卡片右边框闭合、左右留白对称、底部滚动余量以及 100%/150% DPI 下的文字和边框完整性。
- 支持跟随系统、浅色和深色三种主题模式。系统深色主题或控件深色能力不可用时，统一回退到浅色主题。

## 设计系统、主题与国际化

- 所有 UI 的颜色、字号、间距、圆角、边框、控件状态和尺寸必须使用统一设计令牌。新增视觉需求时先补充或调整令牌，页面、弹窗和组件不得自行硬编码视觉值。
- 设计令牌必须通过语义映射支持浅色与深色主题；深色不可用时统一解析为浅色令牌，不允许各页面独立降级。
- 所有 GUI 可见文案，包括侧边栏、按钮、标题、提示、错误和对话框，必须通过 i18n 键映射取得。禁止在 Slint 或 Rust UI 代码中直接写可见文本。
- i18n 文案注入应使用 `I18nSnapshot` 集中构建指定语言的完整文案快照，并通过单一 `apply_to()` 位置维护快照字段到 Slint setter 的映射；禁止在多个调用点重复注入，也不要使用宏替代显式映射。
- 首批必须支持简体中文和英文。翻译缺失时使用已定义的回退文案，不允许展示未解析的键名。
- 主题、设计令牌、i18n 资源与解析、共享控件基线归 `feature/design-system` 负责；`feature/base` 负责其基础接入和共享契约协调。

## 页面导航与状态隔离

- 左侧侧边栏只包含 `欢迎`、`配置`、`检索`、`任务` 四个互斥页面；同一时刻只能显示一个页面。
- 按钮文案必须使用 i18n 映射，侧边栏宽度由当前语言的按钮标签和设计令牌决定，不得按固定字符数或固定像素宽度实现。
- 每个页面拥有独立的临时状态。页面之间不得直接读写、调用或持有对方的状态。
- 跨页面只允许通过已保存且重新读取的配置变更互通；不得将配置页面或其他页面的内存状态作为共享通道。

## 配置持久化与启动参数

- 所有配置必须存储在 SQLite 数据库文件中。
- 程序启动时支持 `-c <路径>` 参数以选择配置数据库文件。默认路径须在后续实现中明确，不得在当前骨架阶段推断。
- 初始数据库仅包含 `config` 表，且该表当前只包含 `version = "0.0.1"` 这一项配置。未确认需求前，不要预置任务、下载历史或 UI 偏好字段。
- 数据库路径解析、初始化、版本读取与迁移归 `feature/storage` 负责；配置页面只能通过稳定的存储接口读写配置。
- 存储模块允许按职责拆分为 `src/storage/` 子模块：`mod.rs` 管理单例和对外接口，`config.rs` 管理配置模型，`database.rs` 管理 SQLite 操作，`path.rs` 管理路径解析，`error.rs` 管理存储错误。

## 数据结构优先

先设计并确认持久化记录、配置快照、页面输入输出、任务状态和允许跨模块流通的数据结构及不变量；再定义 Rust 类型、存储 API、页面模型与函数。不得由 UI 回调、方法或结构类型反向推导领域数据结构。

## 架构

当前代码已包含最小 Rust/Slint 应用架构：`build.rs` 编译 `ui/app-window.slint` 及其导入的设计系统组件，`src/app/window.rs` 通过 `slint::include_modules!()` 创建并运行 `AppWindow`；`src/design_system/` 提供主题解析、Windows 系统主题读取和 i18n catalog；`ui/design/` 提供令牌、主题/i18n 全局对象、共享导航按钮、主布局和页面滚动宿主。

`src/app/contracts.rs` 与 `src/app/navigation.rs` 提供四路由 `Route` 和 `NavigationState` 基础契约。当前共享壳层只提供页面内容插槽，不实现欢迎、配置、检索、任务页面正文。页面接入共享壳层时，必须沿用 `PageScrollHost` 的内容高度契约和左右留白结构；不要在页面组件内重复实现 ScrollView、Flickable 或窗口级宽高计算。后续架构说明应继续以实际 Rust/Slint 源代码和配置为依据，并明确 yt-dlp 调用、桌面界面、SQLite 存储和平台适配之间的边界；不得引入 WebView 作为界面层。

## 分支协作与所有权

- 开发前先确认当前分支的职责、负责模块和已有提交意图。代理只能在自身负责的 `feature/<范围>` 分支开发。
- 每个开发分支一旦修改代码、Slint 界面、构建脚本或 Cargo 配置，必须在该分支重新编译，至少执行一次 `cargo check`。
- 不得跨分支修改、占用或复制其他分支负责的模块、页面、数据库职责和 UI 实现。若请求属于其他分支，只列出目标分支的实施计划，等待用户决定。
- 共享数据结构、主题令牌、i18n 契约、存储 schema 和主窗口路由的变更，必须由负责分支或 `feature/base` 先发布稳定契约；依赖分支只能接入已批准的最小接口。
- 本地 `AGENTS.md` 是拟议分支职责的协作参考，不是受 Git 跟踪的正式规范。

## 测试约束

测试代码必须始终以单一文件独立放置，不得合并到工程项目代码中。新增测试时，应使用项目约定的独立测试文件或测试目录，并避免在生产 Rust 源文件中加入测试模块；在项目结构确定后，补充具体的单文件测试命令。

### Slint 页面验收

- `cargo fmt --check`、`cargo check` 和 `cargo test` 只能验证语法、类型和契约，不能替代真实窗口布局验证。
- 每个新增或修改的页面至少要在 Windows 实际运行一次：验证窄窗口滚轮滚动、宽窗口卡片右边框闭合、左右留白对称、底部滚动余量和链接/按钮 hover 与焦点状态。
- 页面截图或手工验证发现布局异常时，先检查窗口、ScrollView 视口、滚动内容容器和页面组件的几何层级，再修改尺寸值；不要通过连续增加固定宽高或重复传递宽度来试错。

## Git 提交规范

严格遵守 Conventional Commits 规范。提交标题使用 `<类型>[可选范围]: <简体中文描述>` 格式，例如：

```text
feat(gui): 添加下载任务列表界面
fix(download): 修复 yt-dlp 进程退出状态处理
```

类型必须使用规范类型（如 `feat`、`fix`、`refactor`、`test`、`docs`、`build`、`ci`、`chore` 或 `perf`）；提交标题和正文均使用简体中文。提交应聚焦单一变更，避免无关文件混入。

## 仓库专属要求

- 选择开发命令前先检查当前文件树和 Cargo manifest；当前仓库可能仍处于骨架阶段。
- 修改功能时保持 Windows 10 1903+ 支持，不得添加 WebView 依赖或 WebView 实现。
- 所有 GUI 可见文本必须经简体中文和英文的 i18n 映射，且颜色、字号与间距必须使用统一设计令牌。
- 保持测试文件与工程项目代码分离，并遵守单文件测试约束。
- 项目初始化后，持续将本文件与实际命令和架构保持同步。
