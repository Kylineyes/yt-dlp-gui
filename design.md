# UI 设计系统

## 1. 目的与适用范围

本文档是 yt-dlp GUI 的 UI 设计基线，供后续 Rust + Slint 实现使用。它定义主题模式、颜色、字号、字体、行高、字间距、控件状态和无障碍要求，适用于窗口、导航、下载任务、设置页、状态提示、对话框和表单等桌面界面。

本文档不定义业务流程、yt-dlp 调用协议或具体模块边界。UI 必须使用 Slint 实现，不得引入 WebView、HTML、CSS、浏览器内核或相关依赖。

## 2. 设计原则

- **语义优先**：组件引用 `text-primary`、`surface-primary`、`accent` 等语义令牌，不直接依赖 `gray-700` 等原始色值。
- **主题可替换**：主题切换只改变令牌映射，不改变控件结构、内容和交互语义。
- **可读性优先**：正文、错误和关键操作保持清晰可读，辅助信息不得成为核心交互的唯一载体。
- **状态可辨识**：成功、警告、错误、暂停和进行中状态不能只用颜色表达，还要配合文字、图标或形状。
- **桌面优先**：以 Windows 10 1903 及之后版本为目标，兼顾高 DPI、键盘操作和系统字体回退。
- **集中管理**：颜色、字号、字体和间距集中在主题与排版令牌中，避免散落在 `.slint` 组件内。

## 3. 主题模式

### 3.1 模式定义

```text
ThemeMode = Light | Dark | System
```

| 模式 | 行为 |
| --- | --- |
| `Light`（浅色） | 始终使用浅色令牌，不随 Windows 系统主题变化。 |
| `Dark`（深色） | 始终使用深色令牌，不随 Windows 系统主题变化。 |
| `System`（跟随系统） | 使用 Windows 当前浅色/深色偏好，并在系统主题变化时更新。 |

### 3.2 交互与持久化规则

- 默认模式为 `System`。
- 设置页面使用“浅色”“深色”“跟随系统”三个明确选项，不使用含义不明确的“自动”。
- 持久化保存用户选择的 `ThemeMode`，同时单独维护解析后的有效主题 `Light` 或 `Dark`；不能把 `System` 启动时改写成某一个固定主题。
- 启动时先读取用户模式并解析有效主题，再创建主要界面，避免出现明显的浅色闪烁后切换到深色。
- 用户选择 `Light` 或 `Dark` 后，Windows 系统主题变化不影响界面。
- `System` 模式下，Windows 主题变化应更新所有使用主题令牌的组件，包括已打开的弹窗和状态提示。
- 主题切换不得重置文本输入、下载任务、滚动位置或窗口状态。
- 第一版采用即时切换，不要求动画过渡；若未来增加过渡，必须短暂且不能影响交互响应。
- Windows 主题读取失败时，`System` 回退到浅色有效主题，但保留用户的 `System` 选择，下一次可继续尝试读取。

## 4. 颜色令牌

组件只使用下表中的语义令牌。浅色和深色值是初始基线，实际实现仍需通过对比度检查；不得在组件中自行覆盖令牌含义。

| 令牌 | 浅色 | 深色 | 用途 |
| --- | --- | --- | --- |
| `background-primary` | `#F7F8FA` | `#111827` | 窗口主背景 |
| `surface-primary` | `#FFFFFF` | `#1F2937` | 卡片、面板、输入区域 |
| `surface-elevated` | `#FFFFFF` | `#273449` | 弹出层、对话框、悬浮区域 |
| `text-primary` | `#1F2937` | `#F9FAFB` | 标题和正文 |
| `text-secondary` | `#4B5563` | `#D1D5DB` | 辅助说明、次级信息 |
| `text-muted` | `#6B7280` | `#9CA3AF` | 占位文本、非关键元信息 |
| `border-default` | `#D1D5DB` | `#374151` | 默认边框、分隔线 |
| `border-strong` | `#9CA3AF` | `#6B7280` | 强调边框、输入焦点前的高辨识边界 |
| `accent` | `#2563EB` | `#60A5FA` | 主操作、链接、选中态 |
| `accent-hover` | `#1D4ED8` | `#93C5FD` | 主操作悬停态 |
| `accent-pressed` | `#1E40AF` | `#BFDBFE` | 主操作按下态 |
| `accent-on` | `#FFFFFF` | `#111827` | 强调色表面的前景文本或图标 |
| `focus-ring` | `#2563EB` | `#93C5FD` | 键盘焦点环 |
| `success` | `#15803D` | `#4ADE80` | 成功、已完成 |
| `warning` | `#B45309` | `#FBBF24` | 警告、需要注意 |
| `danger` | `#B91C1C` | `#F87171` | 错误、危险操作 |
| `disabled-surface` | `#E5E7EB` | `#374151` | 禁用控件背景 |
| `disabled-text` | `#9CA3AF` | `#6B7280` | 禁用控件文本 |

颜色使用规则：

- 普通文本与背景的对比度目标为至少 4.5:1，大文本至少 3:1，遵循 WCAG AA。
- `accent-on` 必须针对对应的 `accent`、`accent-hover` 和 `accent-pressed` 组合分别验证，不能假设白色或深色前景始终适用。
- `success`、`warning`、`danger` 等状态色必须同时配合图标、文字或形状。
- 焦点环必须与相邻背景有足够对比度，不能只依赖边框颜色的轻微变化。
- 禁用状态可以降低对比度以表达不可交互，但文本仍应保持可辨识。
- 新增颜色时先增加语义令牌和两套主题值，再在组件中使用；禁止直接添加未登记的十六进制色值。

## 5. 基础尺寸令牌

共享 UI 基础设施使用以下语义尺寸令牌，页面和组件不得直接写入未登记的尺寸值：

| 令牌 | 建议值 | 用途 |
| --- | ---: | --- |
| `spacing-small` | 8 px | 控件内部和紧邻元素间距 |
| `spacing-medium` | 16 px | 常规内容间距、按钮水平内边距 |
| `spacing-large` | 24 px | 页面内容和分组间距 |
| `spacing-compact` | 2 px | 表格行之间的紧凑间距 |
| `sidebar-padding` | 20 px | 侧边栏水平内边距 |
| `sidebar-min-width` | 160 px | 侧边栏最小逻辑宽度 |
| `sidebar-gap` | 8 px | 侧边栏按钮垂直间距 |
| `corner-radius-small` | 4 px | 边框和紧凑控件 |
| `corner-radius-medium` | 8 px | 按钮、面板和卡片 |
| `border-width-default` | 1 px | 默认边框 |
| `focus-ring-width` | 2 px | 键盘焦点环 |
| `control-min-height` | 36 px | 常规可操作控件最小高度 |
| `window-min-width` | 640 px | 窗口最小宽度基线 |
| `window-min-height` | 480 px | 窗口最小高度基线 |
| `scroll-content-padding` | 24 px | 页面滚动内容内边距 |
| `scroll-bar-width` | 14 px | 滚动条布局预留宽度 |

这些值是逻辑尺寸，不是物理像素假设。窗口、侧边栏和控件必须在文本放大以及 Windows 100% 和 150% DPI 下重新验证；若当前语言或缩放档位需要更大空间，应由布局自然扩展。

## 6. 文本大小

字号使用 Slint 的逻辑尺寸，不把固定物理像素当作显示器 DPI 假设。初始字号层级如下：

| 令牌 | 建议字号 | 用途 |
| --- | ---: | --- |
| `font-size-caption` | 12 px | 辅助说明、非关键元信息 |
| `font-size-body` | 14 px | 默认正文、表单控件、任务信息 |
| `font-size-body-large` | 16 px | 重要正文、设置项、状态摘要 |
| `font-size-title-small` | 18 px | 区块标题 |
| `font-size-title` | 22 px | 页面标题 |
| `font-size-display` | 28 px | 空状态标题、突出数值 |

规则：

- 默认正文和常规控件使用 `font-size-body`。
- `font-size-caption` 只用于辅助信息，不能用于核心操作、错误信息或唯一的状态说明。
- 组件不得自行引入未登记字号；确有需要时先补充字号令牌。
- 若未来提供文本大小设置，应按比例整体调整正文、标题和辅助文本层级，不只放大单个控件。
- 文本放大后布局必须允许换行或扩展，不能依赖固定宽度截断。

建议的文本大小档位：

| 档位 | 正文缩放 | 适用场景 |
| --- | ---: | --- |
| `Default` | 100% | 默认设置 |
| `Large` | 115% | 需要更大阅读尺寸的用户 |
| `ExtraLarge` | 130% | 较远距离阅读或视觉辅助 |

实现文本大小档位时，优先保持字号层级比例；若某个控件无法容纳放大文本，应扩大布局或允许换行，而不是降低文本可读性。

## 6. 字体选择

### 6.1 默认字体栈

- **默认 UI 字体**：`Segoe UI`。
- **简体中文回退**：`Microsoft YaHei UI`。
- **跨环境中文回退**：`Noto Sans CJK SC`，仅在系统已安装时使用。
- **最终回退**：系统 sans-serif 回退。
- **等宽文本**：`Cascadia Mono`；不可用时回退到 `Consolas`，再回退到系统等宽字体。

字体规则：

- 字体选择集中在主题或排版配置中，不在各个 `.slint` 组件里硬编码。
- 不依赖 Web 字体下载，应用离线运行时仍必须有可用字体。
- 目标机器未安装可选字体时必须平稳回退，不得导致启动失败或文字消失。
- 文件名、路径、命令和日志使用等宽字体；普通正文和设置项使用 UI 字体。
- 中文、英文、数字、混合标点和长路径都要验证字形覆盖与对齐。
- 如果未来提供字体设置，至少提供“系统默认”和“适合中文阅读”两个稳定选项；选择不可用时回退到默认字体。

## 7. 行高与字间距

| 令牌 | 建议值 | 用途 |
| --- | ---: | --- |
| `line-height-body` | 1.45 × 字号 | 正文、多行说明、错误详情 |
| `line-height-compact` | 1.25 × 字号 | 单行控件、表格、任务列表 |
| `line-height-title` | 1.20 × 字号 | 页面标题、区块标题 |
| `letter-spacing-default` | 0 | 常规正文和控件文本 |
| `letter-spacing-heading` | 0 | 标题；只有实际渲染更清晰时才调整 |

实现规则：

- 行高和字间距通过统一排版令牌或可复用文本容器实现。
- 若当前 Slint 版本不直接支持行高或字间距属性，应使用统一的文本容器高度、上下内边距和布局组件实现，不能插入空格或换行符模拟字间距。
- 正文、多行提示和错误详情允许自然换行。
- 字号、行高调整后不得出现文字垂直裁切。
- 中文标点、英文混排、数字、路径和长 URL 需要单独验证。

## 8. 控件状态与组件规则

所有交互控件至少应区分以下状态：默认、悬停、按下、键盘焦点、禁用、错误和选中。状态表现同时考虑背景、边框、文本、图标和可用的非颜色线索。

- 所有颜色和字号从集中式主题对象或令牌映射读取。
- 主要操作使用 `accent` 系列，危险操作使用 `danger` 系列，取消操作保持中性，不把危险操作伪装成主操作。
- 输入框在默认、获得焦点和错误状态下都应有可识别边界。
- 禁用状态降低交互性和视觉权重，但不能让文本完全不可读。
- 下载“成功”“失败”“暂停”“进行中”等业务状态引用语义状态令牌，并同时显示文字或图标。
- 主题判断只在主题映射层出现；组件内部不得各自实现 `Light`/`Dark` 分支。
- 新增组件前先检查现有令牌是否足够；需要新增令牌时先更新本文档。

## 9. 无障碍与显示适配

- 普通文本对比度至少达到 4.5:1，大文本至少达到 3:1。
- 键盘操作必须有清晰可见的 `focus-ring`，且焦点不能仅依赖鼠标悬停样式。
- 颜色不是传达成功、警告、错误、暂停或下载进度的唯一方式。
- 关键操作具备明确的文字标签或可访问名称，图标按钮不能只有视觉含义。
- 常规可操作区域建议不小于 32 × 32 逻辑像素；主要操作或需要更易点击的区域应使用更大尺寸。
- Windows 100%、125%、150% 和 200% 缩放下，文字不得裁切，控件不得重叠，焦点顺序不得改变。
- 文本放大后不得遮挡按钮、输入框或状态信息。
- Windows 高对比度设置、系统字体缺失和异常字体回退至少要有安全降级路径。
- 后续 Slint 版本或辅助功能接口支持时，应补充无障碍树、键盘顺序和读屏名称验证。

## 10. Slint 落地约定

主题系统建议拆成两层：

1. **Rust 侧状态与系统适配**
   - 保存用户选择的 `ThemeMode` 和文本/字体设置。
   - 读取 Windows 系统浅色/深色偏好并监听变化。
   - 在启动创建 UI 前解析有效主题，系统读取失败时按规则回退。
   - 将用户设置持久化与 UI 渲染解耦。

2. **Slint 侧主题与排版令牌**
   - 定义集中式主题对象，包含 `mode`、解析后的有效主题和所有颜色、字号、字体、行高、字间距属性。
   - 组件只绑定主题对象的语义属性，不直接散落十六进制颜色、字号或字体名。
   - 主题变化通过共享属性更新组件，不重建窗口，不重置业务状态。
   - 根窗口必须将标准控件 `Palette.color-scheme` 同步到解析后的有效主题，避免应用主题与操作系统主题不一致时出现控件文字和背景对比度不足。
   - 需要新增令牌时，先更新本文档，再实现对应的主题属性和组件使用。

后续建立工程后，主题实现应保持 Windows 10 1903+ 兼容，不引入 WebView 或浏览器技术栈。本文档中的令牌名称是设计接口的一部分；实现可以调整内部数据结构，但不得悄然改变语义和对比度目标。

## 11. 验收清单

每次新增或修改 UI、主题逻辑或排版设置时，至少检查：

- [ ] 浅色、深色和跟随系统模式行为符合第 3 节。
- [ ] 每个使用的颜色、字号、字体和间距都来自集中式令牌。
- [ ] 颜色令牌在两种主题下都有定义，并通过对比度检查。
- [ ] 默认、悬停、按下、焦点、禁用、错误和选中状态完整。
- [ ] 中英文混排、中文长句、数字、长路径和长 URL 不裁切。
- [ ] 文本放大和 Windows 高 DPI 缩放不会破坏布局。
- [ ] 关键状态不依赖颜色单独表达，键盘焦点清晰可见。
- [ ] 字体缺失时能够回退，应用不依赖网络字体。
- [ ] 未引入 WebView、HTML、CSS 或浏览器内核依赖。
